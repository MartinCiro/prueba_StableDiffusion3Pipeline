services:
  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data

  sd3-app:
    build: .
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - HF_TOKEN=${HF_TOKEN}
      - DEVICE=cpu
    volumes:
      # Montar venv local en el contenedor
      - ./venv:/app/venv
      # Montar código fuente
      - ./src:/app/src
      # Directorios de trabajo
      - ./outputs:/app/outputs
      - ./models:/app/models
    command: python /app/src/main.py

volumes:
  redis_data: